input {
	beats {
		port => 5044
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "10.0.0.232:9200"
		user => "elastic"
		ecs_compatibility => disabled
	}
}
